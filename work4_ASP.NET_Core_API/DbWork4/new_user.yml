info:
  name: new_user
  type: http
  seq: 5

http:
  method: POST
  url: https://localhost:7203/api/users
  body:
    type: json
    data: |-
      {
      "username": "john_doe",
      "age": 25
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
