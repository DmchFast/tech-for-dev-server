info:
  name: Basic Auth
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/login-basic"
  auth:
    type: basic
    username: DD
    password: "123"

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:5161

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
