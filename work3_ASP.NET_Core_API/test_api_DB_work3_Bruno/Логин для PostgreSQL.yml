info:
  name: Логин для PostgreSQL
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/db/login"
  body:
    type: json
    data: |-
      {
        "username": "di",
        "password": "123"
      }
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:5161

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
